{% extends 'base.html.twig' %} {% block title %}New Editor{% endblock %} {%
block body %}
<turbo-frame id="id_editor">
	<h1>Create new Editor</h1>

	{{ include("editor/_form.html.twig") }}

	{% for message in app.flashes('errorEditor') %}
	<div class="error-message">{{ message }}</div>
	{% endfor %}

	<a href="{{ path('app_editor_index') }}">back to list</a>
</turbo-frame>
{% endblock %}
